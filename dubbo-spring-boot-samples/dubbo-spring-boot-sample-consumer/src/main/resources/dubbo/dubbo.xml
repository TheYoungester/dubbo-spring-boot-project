<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://code.alibabatech.com/schema/dubbo
		http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <dubbo:registry
            address="${dubbo.application.id}" id="universal-zk"/>
    <dubbo:reference interface="com.alibaba.boot.dubbo.demo.consumer.DemoService" version="${demo.service.version}" application = "${dubbo.application.id}" registry="${universal-zk}" id="demoService"/>
</beans>